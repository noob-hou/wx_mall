.tab-title{
  display: flex;
  height: 80rpx;
}
.tab-title view{
  flex: 1;
  text-align: center;
  line-height: 80rpx;
}
.active{
  color: var(--themeColor);
  border-bottom: 6rpx solid var(--themeColor);
}